-- LEGACY / to-rewrite. Review-notes (<leader>r*).
-- Remove this folder + its require() line in plugins/init.lua to drop it.
local M = require("review-notes.core")
require("review-notes.keymaps")
return M
