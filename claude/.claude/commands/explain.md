---
description: Generate visual HTML explanation with diagrams and quizzes
---

Use explainer agent. Generate visual HTML explanation built on the shared `report` scaffold (dark theme + light toggle, glossary/citation hovercards, in-memory annotation tool, zoomable Mermaid with legend) plus quizzes.

Topic: $ARGUMENTS

No topic? Ask user what to explain.

When the explainer agent returns the saved path, **automatically open it in the browser** (`open <path>` on macOS, `xdg-open` on Linux) — don't make the user open it.
