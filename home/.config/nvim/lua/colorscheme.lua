-- https://github.com/yorik1984/newpaper.nvim

require('newpaper').setup({
    style = 'light'
})
--
-- local Color, colors, Group, groups, styles = require('colorbuddy').setup()
--
-- Color.new('red', '#D8335B')
-- Color.new('grey27', '#a0a0a0')
--
-- Group.new('Error', colors.red, nil, styles.bold)
-- Group.new('Comment', colors.red, nil, styles.bold)
-- Group.new('ColorColumn', colors.red, nil, styles.bold)
